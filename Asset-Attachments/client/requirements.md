## Packages
framer-motion | Smooth transitions for state updates and severity changes
date-fns | Formatting timestamps for logs and reports

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
  mono: ["var(--font-mono)"],
}
